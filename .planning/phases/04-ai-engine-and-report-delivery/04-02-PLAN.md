---
phase: 04-ai-engine-and-report-delivery
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - app/bot/handlers/full_scan.py
  - tests/test_full_scan_ai_delivery.py
autonomous: false
requirements:
  - BOT-08
  - AI-04

must_haves:
  truths:
    - "After questionnaire completion the bot sends 'Генерирую отчёт...' before the API call"
    - "Bot sends 7 separate Telegram messages: one per block (6 blocks) plus one numerology message"
    - "Each block message starts with a bold Russian header (e.g. *Архитектура*)"
    - "scan.report is stored as JSONB dict in the database before any delivery message is sent"
    - "scan.status is set to ScanStatus.completed after report is stored"
    - "If FullScanAIService raises an exception, bot sends error message and sets scan.status=failed"
  artifacts:
    - path: "app/bot/handlers/full_scan.py"
      provides: "generate_and_deliver_report() coroutine called from _advance_to_next when all questions done"
      contains: "generate_and_deliver_report"
    - path: "tests/test_full_scan_ai_delivery.py"
      provides: "Unit tests for report delivery flow"
  key_links:
    - from: "app/bot/handlers/full_scan.py"
      to: "app/services/full_scan_ai_service.py"
      via: "FullScanAIService().generate_full_report(answers, birth_date, scan_type)"
      pattern: "FullScanAIService"
    - from: "app/bot/handlers/full_scan.py"
      to: "app/services/scan_service.py"
      via: "scan_service.complete_full_scan(scan_id, report, token_usage)"
      pattern: "complete_full_scan"
    - from: "app/bot/handlers/full_scan.py"
      to: "bot.send_message"
      via: "7 sequential send_message calls (1 numerology + 6 blocks)"
      pattern: "send_message.*7|for.*BLOCK"
---

<objective>
Wire the full scan AI service into the questionnaire completion hook and deliver the report as split Telegram messages.

Purpose: Plan 01 built the AI engine in isolation. This plan connects it to the user-facing flow: when the last question is answered, the bot generates the Claude Sonnet report, stores it as JSONB, and sends 7 structured messages.

Output:
- app/bot/handlers/full_scan.py — _advance_to_next() calls generate_and_deliver_report() instead of "Запускаю анализ..."
- tests/test_full_scan_ai_delivery.py — mocked delivery tests
</objective>

<execution_context>
@/Users/yulya-reva/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yulya-reva/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-engine-and-report-delivery/04-01-SUMMARY.md

@app/bot/handlers/full_scan.py
@app/services/full_scan_ai_service.py
@app/services/scan_service.py
@app/models/scan.py
@tests/conftest.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From app/services/full_scan_ai_service.py (created in Plan 01):
```python
class FullScanAIService:
    async def generate_full_report(
        self,
        answers: dict,
        birth_date: date,
        scan_type: str,
    ) -> dict:
        """Returns dict with keys:
          архитектура, слепые_зоны, энергетические_блоки, команда, деньги, рекомендации,
          numerology (soul_number, life_path_number, birth_date),
          token_usage (input_tokens, output_tokens)
        """

BLOCK_KEYS = ["архитектура", "слепые_зоны", "энергетические_блоки", "команда", "деньги", "рекомендации"]
```

From app/services/scan_service.py (added in Plan 01):
```python
async def complete_full_scan(self, scan_id: int, report: dict, token_usage: dict) -> Scan:
    """Stores report as JSONB, sets status=completed, sets completed_at."""
```

From app/models/scan.py:
```python
class ScanStatus(str, Enum):
    questionnaire_complete = "questionnaire_complete"
    completed = "completed"
    failed = "failed"
```

From app/bot/handlers/full_scan.py — current questionnaire completion in _advance_to_next():
```python
if next_index >= total:
    scan_service = ScanService(session)
    await scan_service.complete_questionnaire(scan_id)
    await state.set_state(FullScanStates.completing)
    await bot.send_message(chat_id, "Анкета заполнена! Запускаю анализ...")
    await state.clear()
```

From app/bot/handlers/mini_scan.py — existing pattern to avoid circular import:
```python
# Uses message_obj.bot to avoid circular import with app.main
```

Scan answers dict access pattern from scan_service:
```python
scan = await scan_service.get_scan(scan_id)
scan.answers  # dict
# birth_date stored as ISO string: "1990-05-15"
```

From app/services/user_service.py (to get birth_date from user profile as fallback):
```python
async def get_or_create(telegram_id, username, full_name) -> tuple[User, bool]
# user.birth_date is a date object or None
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire generate_and_deliver_report into _advance_to_next and implement split delivery</name>
  <files>app/bot/handlers/full_scan.py, tests/test_full_scan_ai_delivery.py</files>

  <read_first>
    - app/bot/handlers/full_scan.py (full file — especially _advance_to_next() at line 87-112)
    - app/services/full_scan_ai_service.py (generate_full_report signature and BLOCK_KEYS)
    - app/services/scan_service.py (complete_full_scan signature, get_scan for answers retrieval)
    - app/bot/handlers/mini_scan.py (how _generate_and_send_report avoids circular import, how it uses message_obj.bot)
    - tests/test_mini_scan_flow.py (handler testing pattern with mocked bot and session)
  </read_first>

  <behavior>
    - generate_and_deliver_report() is an async function (not a method) defined in full_scan.py
    - Signature: async def generate_and_deliver_report(bot: Bot, chat_id: int, scan_id: int, scan_type: str, session: AsyncSession) -> None
    - Step 1: await bot.send_message(chat_id, "Генерирую отчёт... Это займёт около 30 секунд.")
    - Step 2: scan = await scan_service.get_scan(scan_id); answers = scan.answers or {}
    - Step 3: extract birth_date from answers["birth_date"] (ISO string) using date.fromisoformat(); if missing or ValueError → send error message, set scan.status = ScanStatus.failed, return early
    - Step 4: report = await FullScanAIService().generate_full_report(answers, birth_date, scan_type)
    - Step 5: token_usage = report.get("token_usage", {})
    - Step 6: await scan_service.complete_full_scan(scan_id, report, token_usage) — stores JSONB and sets completed
    - Step 7: send numerology message first:
      "🔢 *Нумерология*\nЧисло души: {soul_number}\nЧисло жизненного пути: {life_path_number}"
      parse_mode="Markdown"
    - Step 8: for each key in BLOCK_KEYS, format header as title-cased Russian label (map: архитектура→Архитектура, слепые_зоны→Слепые зоны, энергетические_блоки→Энергетические блоки owner'а, команда→Команда, деньги→Деньги, рекомендации→Рекомендации)
      send: "*{label}*\n\n{report[key]}" with parse_mode="Markdown"
    - On ANY exception from FullScanAIService.generate_full_report(): log error, set scan.status = ScanStatus.failed, await session.commit(), await bot.send_message(chat_id, "Произошла ошибка при генерации отчёта. Пожалуйста, попробуйте позже или обратитесь в поддержку.")
    - In _advance_to_next(), replace the current "Запускаю анализ..." block with:
      await bot.send_message(chat_id, "Анкета заполнена!")
      await state.clear()
      await generate_and_deliver_report(bot, chat_id, scan_id, scan_type, session)
    - Import from full_scan_ai_service: FullScanAIService, BLOCK_KEYS
    - Import date from datetime (for date.fromisoformat)

    Tests (tests/test_full_scan_ai_delivery.py):
    - test_generate_and_deliver_report_happy_path: mock FullScanAIService.generate_full_report to return full report dict; mock scan_service.get_scan to return scan with answers including birth_date="1990-05-15"; mock scan_service.complete_full_scan; mock bot.send_message; assert bot.send_message called 8 times total (1 "Генерирую" + 1 numerology + 6 blocks)
    - test_generate_and_deliver_report_missing_birth_date: answers without birth_date key; assert scan.status set to failed, send_message called with error text
    - test_generate_and_deliver_report_ai_failure: FullScanAIService.generate_full_report raises ValueError; assert scan.status set to failed, error message sent
    - test_block_messages_contain_headers: assert each of the 6 send_message calls after numerology contains its Russian block header
  </behavior>

  <action>
In app/bot/handlers/full_scan.py, add these imports at the top:
```python
from datetime import date
from app.services.full_scan_ai_service import FullScanAIService, BLOCK_KEYS
```

Add BLOCK_LABELS dict after imports:
```python
_BLOCK_LABELS = {
    "архитектура": "Архитектура",
    "слепые_зоны": "Слепые зоны",
    "энергетические_блоки": "Энергетические блоки owner'а",
    "команда": "Команда",
    "деньги": "Деньги",
    "рекомендации": "Рекомендации",
}
```

Add generate_and_deliver_report() before _send_question():
```python
async def generate_and_deliver_report(
    bot: Bot,
    chat_id: int,
    scan_id: int,
    scan_type: str,
    session: AsyncSession,
) -> None:
    """Generate full AI report and deliver as split Telegram messages."""
    scan_service = ScanService(session)

    await bot.send_message(chat_id, "Генерирую отчёт... Это займёт около 30 секунд.")

    scan = await scan_service.get_scan(scan_id)
    answers = scan.answers or {}

    birth_date_str = answers.get("birth_date", "")
    try:
        birth_date = date.fromisoformat(birth_date_str)
    except (ValueError, TypeError):
        logger.error("Missing or invalid birth_date for scan_id=%s: %r", scan_id, birth_date_str)
        scan.status = ScanStatus.failed.value
        await session.commit()
        await bot.send_message(
            chat_id,
            "Не удалось определить дату рождения. Пожалуйста, начните скан заново (/start).",
        )
        return

    try:
        ai_service = FullScanAIService()
        report = await ai_service.generate_full_report(answers, birth_date, scan_type)
    except Exception:
        logger.exception("AI generation failed for scan_id=%s", scan_id)
        scan.status = ScanStatus.failed.value
        await session.commit()
        await bot.send_message(
            chat_id,
            "Произошла ошибка при генерации отчёта. Пожалуйста, попробуйте позже или обратитесь в поддержку.",
        )
        return

    token_usage = report.get("token_usage", {})
    await scan_service.complete_full_scan(scan_id, report, token_usage)

    # Deliver numerology block first
    numerology = report.get("numerology", {})
    await bot.send_message(
        chat_id,
        (
            f"*Нумерология*\n"
            f"Число души: {numerology.get('soul_number', '—')}\n"
            f"Число жизненного пути: {numerology.get('life_path_number', '—')}"
        ),
        parse_mode="Markdown",
    )

    # Deliver 6 content blocks
    for key in BLOCK_KEYS:
        label = _BLOCK_LABELS[key]
        content = report.get(key, "недостаточно данных для анализа этого аспекта")
        await bot.send_message(
            chat_id,
            f"*{label}*\n\n{content}",
            parse_mode="Markdown",
        )
```

Modify _advance_to_next() — replace the `if next_index >= total:` block:
```python
if next_index >= total:
    scan_service = ScanService(session)
    await scan_service.complete_questionnaire(scan_id)
    await state.set_state(FullScanStates.completing)
    await bot.send_message(chat_id, "Анкета заполнена!")
    await state.clear()
    await generate_and_deliver_report(bot, chat_id, scan_id, scan_type, session)
```

Write tests in tests/test_full_scan_ai_delivery.py using AsyncMock for bot and session, patch("app.bot.handlers.full_scan.FullScanAIService") to mock the AI call.
  </action>

  <verify>
    <automated>cd /Users/yulya-reva/reva-scanner && python -m pytest tests/test_full_scan_ai_delivery.py -v 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "generate_and_deliver_report" app/bot/handlers/full_scan.py` returns at least 2 matches (definition + call in _advance_to_next)
    - `grep -n "Генерирую отчёт" app/bot/handlers/full_scan.py` returns a match
    - `grep -n "FullScanAIService" app/bot/handlers/full_scan.py` returns a match in the import and in the function body
    - `grep -n "complete_full_scan" app/bot/handlers/full_scan.py` returns a match
    - `grep -n "ScanStatus.failed" app/bot/handlers/full_scan.py` returns at least 2 matches (birth_date error path + AI error path)
    - `grep -n "parse_mode" app/bot/handlers/full_scan.py` returns at least 7 matches (numerology + 6 blocks)
    - `grep -n "BLOCK_LABELS\|_BLOCK_LABELS" app/bot/handlers/full_scan.py` returns a match
    - All 4 tests in tests/test_full_scan_ai_delivery.py pass
    - `python -m pytest tests/ -v 2>&1 | grep -c FAILED` returns 0
  </acceptance_criteria>

  <done>generate_and_deliver_report() is wired into _advance_to_next, stores JSONB report, sends 8 messages total (1 status + 1 numerology + 6 blocks), handles missing birth_date and AI errors gracefully, and all tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full report delivery pipeline: FullScanAIService generates a 6-block Claude Sonnet report with numerology, ScanService.complete_full_scan stores it as JSONB, and the handler delivers 8 split Telegram messages.
  </what-built>
  <how-to-verify>
    This is a live Claude API call with real costs (~$0.05-0.15 per test depending on Sonnet pricing). Proceed only when comfortable spending on a test scan.

    Option A — Integration test against real Claude:
    1. Create a test script tests/integration_test_full_report.py that:
       - Builds a mock answers dict with all 15 personal question keys filled
       - Sets birth_date="1990-05-15"
       - Calls FullScanAIService().generate_full_report(answers, date(1990,5,15), "personal")
       - Prints each block with its key
       - Prints token usage
    2. Run: ANTHROPIC_API_KEY=... python tests/integration_test_full_report.py
    3. Verify:
       - All 6 block keys present in output
       - "Нумерология" section shows soul_number and life_path_number specific to 1990-05-15
       - No block says "не удалось" or generic filler (blocks should be substantive for filled-in answers)
       - token_usage shows input_tokens and output_tokens
       - output_tokens < 2000 (sanity check for cost)

    Option B — Manual bot test (requires Phase 1 infrastructure):
    1. Run the bot locally
    2. Start a full scan, answer all questions
    3. Verify: bot sends "Генерирую отчёт...", then numerology message, then 6 block messages
    4. Check database: scan.report is a JSONB dict with all 6 keys
  </how-to-verify>
  <resume-signal>Type "approved" if output looks correct, or describe any issues with the blocks/numerology/formatting</resume-signal>
</task>

</tasks>

<verification>
Run the full test suite after both tasks:

```bash
cd /Users/yulya-reva/reva-scanner && python -m pytest tests/ -v 2>&1 | tail -40
```

Spot-check JSONB type on report column:
```bash
grep -n "report" /Users/yulya-reva/reva-scanner/app/models/scan.py
```

Confirm delivery function wired:
```bash
grep -n "generate_and_deliver_report\|FullScanAIService\|complete_full_scan" /Users/yulya-reva/reva-scanner/app/bot/handlers/full_scan.py
```
</verification>

<success_criteria>
1. Bot sends 8 messages after questionnaire completion: status + numerology + 6 blocks.
2. Each block message has a bold Russian header.
3. Numerology values (soul_number, life_path_number) are specific to the user's birth date.
4. scan.report is stored as JSONB dict before delivery starts.
5. Missing birth_date or AI failure sets scan.status=failed and sends a user-visible error message.
6. All tests pass (pytest tests/ exits 0).
7. Human checkpoint approved.
</success_criteria>

<output>
After completion, create .planning/phases/04-ai-engine-and-report-delivery/04-02-SUMMARY.md
</output>
