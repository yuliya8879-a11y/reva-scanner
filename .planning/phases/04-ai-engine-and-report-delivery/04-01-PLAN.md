---
phase: 04-ai-engine-and-report-delivery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/full_scan_ai_service.py
  - app/models/scan.py
  - app/services/scan_service.py
  - tests/test_full_scan_ai_service.py
autonomous: true
requirements:
  - AI-02
  - AI-03
  - AI-04

must_haves:
  truths:
    - "generate_full_report() returns a dict with exactly these six keys: архитектура, слепые_зоны, энергетические_блоки, команда, деньги, рекомендации"
    - "Each block value is a non-empty string (may contain the phrase 'недостаточно данных' when input is thin)"
    - "The returned dict also contains a numerology key with soul_number, life_path_number, and birth_date"
    - "The returned dict contains a token_usage key with input_tokens and output_tokens"
    - "scan.report is a JSONB column (not Text) — SQLAlchemy type is postgresql.JSONB"
    - "ScanService.complete_full_scan() stores JSONB report dict and sets status to ScanStatus.completed"
  artifacts:
    - path: "app/services/full_scan_ai_service.py"
      provides: "FullScanAIService class with async generate_full_report() method"
      exports: ["FullScanAIService", "calculate_life_path_number"]
    - path: "app/models/scan.py"
      provides: "report column as JSONB not Text"
      contains: "JSONB"
    - path: "app/services/scan_service.py"
      provides: "complete_full_scan() method"
      exports: ["complete_full_scan"]
    - path: "tests/test_full_scan_ai_service.py"
      provides: "Unit tests for AI service and numerology"
  key_links:
    - from: "app/services/full_scan_ai_service.py"
      to: "anthropic.AsyncAnthropic"
      via: "await self._client.messages.create(model='claude-sonnet-4-5')"
      pattern: "claude-sonnet"
    - from: "app/services/full_scan_ai_service.py"
      to: "app/services/numerology.py"
      via: "calculate_soul_number() and calculate_life_path_number()"
      pattern: "calculate_soul_number|calculate_life_path_number"
    - from: "app/services/scan_service.py"
      to: "app/models/scan.py"
      via: "scan.report = report_dict (dict assigned to JSONB column)"
      pattern: "scan\\.report ="
---

<objective>
Build the full scan AI service that generates the 6-block Claude Sonnet report with numerology, and update the data layer to store reports as JSONB.

Purpose: This is the core AI engine. Everything in Phase 4 depends on this service existing and returning a well-typed dict. The handler wiring in Plan 02 imports and calls this directly.

Output:
- app/services/full_scan_ai_service.py — FullScanAIService with generate_full_report()
- app/models/scan.py — report column changed from Text to JSONB
- app/services/scan_service.py — complete_full_scan() method added
- tests/test_full_scan_ai_service.py — unit tests (mocked Claude, real numerology)
</objective>

<execution_context>
@/Users/yulya-reva/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yulya-reva/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/services/ai_service.py
@app/services/numerology.py
@app/models/scan.py
@app/services/scan_service.py
@app/bot/questions.py
@tests/conftest.py
@tests/test_scan_service.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From app/services/numerology.py:
```python
def calculate_soul_number(birth_date: date) -> int:
    """Sums ALL digits of YYYYMMDD, reduces to 1-9."""
```

From app/services/ai_service.py (pattern to replicate):
```python
class AIService:
    def __init__(self) -> None:
        self._client = anthropic.AsyncAnthropic(api_key=settings.anthropic_api_key)

    async def generate_mini_report(self, answers: dict, soul_number: int) -> tuple[str, dict]:
        response = await self._client.messages.create(
            model="claude-haiku-4-20250514",
            max_tokens=500,
            system=_SYSTEM_PROMPT,
            messages=[{"role": "user", "content": user_prompt}],
        )
        usage = response.usage
        usage_dict = {"input_tokens": usage.input_tokens, "output_tokens": usage.output_tokens}
        return response.content[0].text, usage_dict
```

From app/models/scan.py (current — report is Text, must become JSONB):
```python
report: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
# MUST become:
report: Mapped[Optional[dict]] = mapped_column(JSONB, nullable=True)
```

From app/services/scan_service.py (method signature to add):
```python
async def complete_full_scan(self, scan_id: int, report: dict, token_usage: dict) -> Scan:
    """Store JSONB report, set status=completed, set completed_at."""
```

From app/models/scan.py — ScanStatus:
```python
class ScanStatus(str, Enum):
    questionnaire_complete = "questionnaire_complete"
    completed = "completed"
```

Answer keys available from app/bot/questions.py:
Personal (15 questions): birth_date, name, business_area, business_age, role, team_size,
  client_source, avg_check, main_pain, growth_blocker, superpower, decision_style,
  year_goal, current_situation, social_url
Business (12 questions): birth_date, name, business_area, business_age, role, team_size,
  client_source, avg_check, main_pain, growth_blocker, product_description, social_url
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build full_scan_ai_service.py with numerology and 6-block report generation</name>
  <files>app/services/full_scan_ai_service.py, tests/test_full_scan_ai_service.py</files>

  <read_first>
    - app/services/ai_service.py (exact pattern to replicate for Sonnet)
    - app/services/numerology.py (calculate_soul_number — extend with life_path_number here)
    - app/config.py (settings.anthropic_api_key access pattern)
    - app/bot/questions.py (all answer keys for prompt construction)
    - tests/conftest.py (env var setup pattern for tests)
    - tests/test_scan_service.py (AsyncMock / MagicMock test pattern)
  </read_first>

  <behavior>
    - calculate_life_path_number(birth_date: date) -> int: sum day digits + sum month digits + sum year digits separately, then reduce each to single digit, sum those three reduced numbers, reduce again to single digit. Example: 15.05.1990 → day=1+5=6, month=5, year=1+9+9+0=19→10→1; 6+5+1=12→3.
    - generate_full_report(answers: dict, birth_date: date, scan_type: str) -> dict returns EXACTLY:
      {
        "архитектура": str,
        "слепые_зоны": str,
        "энергетические_блоки": str,
        "команда": str,
        "деньги": str,
        "рекомендации": str,
        "numerology": {"soul_number": int, "life_path_number": int, "birth_date": str (ISO)},
        "token_usage": {"input_tokens": int, "output_tokens": int},
      }
    - When answers dict has empty string or missing key for a section → Claude system prompt instructs model to write "недостаточно данных" explicitly.
    - Model used: "claude-sonnet-4-5" (verify at https://docs.anthropic.com/en/docs/about-claude/models)
    - max_tokens: 4000 (leaves budget room for $0.40 cap with Sonnet pricing)
    - Claude response must be parsed as JSON: system prompt instructs Claude to respond with a JSON object with exactly those 6 Russian keys.
    - If JSON parsing fails → raise ValueError with the raw response text included.
    - Token usage logged at INFO level: "Full-scan tokens: input=%s, output=%s"
    - Test: mock anthropic.AsyncAnthropic — make _client.messages.create return a mock with content[0].text = valid JSON string and usage with input_tokens=100, output_tokens=200.
    - Test: calculate_life_path_number(date(1990, 5, 15)) == 3
    - Test: generate_full_report with thin answers (all empty strings) still returns dict with all 6 keys.
    - Test: generate_full_report raises ValueError when Claude returns non-JSON.
  </behavior>

  <action>
Create app/services/full_scan_ai_service.py:

```python
"""AI service for generating full 6-block scan reports via Claude Sonnet."""
from __future__ import annotations
import json
import logging
from datetime import date
from typing import Optional
import anthropic
from app.config import settings
from app.services.numerology import calculate_soul_number

logger = logging.getLogger(__name__)

BLOCK_KEYS = ["архитектура", "слепые_зоны", "энергетические_блоки", "команда", "деньги", "рекомендации"]

_SYSTEM_PROMPT = """Ты — AI-сканер бизнеса «Глаз Бога». Проведи глубокий структурированный анализ бизнеса по ответам пользователя.

Ответь СТРОГО в формате JSON — ровно один объект с 6 ключами на русском языке:
{
  "архитектура": "...",
  "слепые_зоны": "...",
  "энергетические_блоки": "...",
  "команда": "...",
  "деньги": "...",
  "рекомендации": "..."
}

Правила для каждого блока:
- "архитектура": структура бизнеса, бизнес-модель, как устроен поток создания ценности
- "слепые_зоны": что owner предпочитает не замечать — конкретные, неудобные наблюдения
- "энергетические_блоки": психологические паттерны owner'а, страхи, ограничивающие убеждения, нумерологический профиль
- "команда": состояние команды или соло-работы, кадровые риски
- "деньги": финансовая архитектура, средний чек, источники, риски, потенциал роста
- "рекомендации": 3-5 конкретных шагов на ближайшие 90 дней

ВАЖНО:
- Тон прямой, на «ты», без комплиментов
- Если по конкретному блоку данных недостаточно — напиши ровно: "недостаточно данных для анализа этого аспекта"
- Не выдумывай факты которых нет в ответах
- Используй числа (число души и жизненного пути) для персонализации блока энергетические_блоки
- Никакого текста вне JSON — только объект
"""


def calculate_life_path_number(birth_date: date) -> int:
    """Calculate life path number: reduce day, month, year separately then sum and reduce.

    Example: 15.05.1990
      day: 1+5=6
      month: 0+5=5
      year: 1+9+9+0=19 -> 1+0=1 (wait, actually 19 -> 1+9=10 -> 1+0=1)
      sum: 6+5+1=12 -> 1+2=3
    """
    def reduce_to_single(n: int) -> int:
        while n > 9:
            n = sum(int(d) for d in str(n))
        return n if n > 0 else 9

    day_reduced = reduce_to_single(sum(int(d) for d in str(birth_date.day)))
    month_reduced = reduce_to_single(sum(int(d) for d in str(birth_date.month)))
    year_reduced = reduce_to_single(sum(int(d) for d in str(birth_date.year)))
    return reduce_to_single(day_reduced + month_reduced + year_reduced)


class FullScanAIService:
    """Claude Sonnet wrapper for generating full 6-block diagnostic reports."""

    def __init__(self) -> None:
        self._client = anthropic.AsyncAnthropic(api_key=settings.anthropic_api_key)

    async def generate_full_report(
        self,
        answers: dict,
        birth_date: date,
        scan_type: str,
    ) -> dict:
        """Generate a structured 6-block report.

        Returns dict with keys: архитектура, слепые_зоны, энергетические_блоки,
        команда, деньги, рекомендации, numerology, token_usage.
        """
        soul_number = calculate_soul_number(birth_date)
        life_path_number = calculate_life_path_number(birth_date)

        user_prompt = _build_user_prompt(answers, soul_number, life_path_number, scan_type)

        response = await self._client.messages.create(
            model="claude-sonnet-4-5",
            max_tokens=4000,
            system=_SYSTEM_PROMPT,
            messages=[{"role": "user", "content": user_prompt}],
        )

        usage = response.usage
        logger.info(
            "Full-scan tokens: input=%s, output=%s",
            usage.input_tokens,
            usage.output_tokens,
        )

        raw_text = response.content[0].text
        try:
            blocks = json.loads(raw_text)
        except json.JSONDecodeError as exc:
            raise ValueError(
                f"Claude returned non-JSON for full scan report: {raw_text[:200]}"
            ) from exc

        return {
            **{key: blocks.get(key, "недостаточно данных для анализа этого аспекта") for key in BLOCK_KEYS},
            "numerology": {
                "soul_number": soul_number,
                "life_path_number": life_path_number,
                "birth_date": birth_date.isoformat(),
            },
            "token_usage": {
                "input_tokens": usage.input_tokens,
                "output_tokens": usage.output_tokens,
            },
        }


def _build_user_prompt(answers: dict, soul_number: int, life_path_number: int, scan_type: str) -> str:
    """Build the user-facing prompt from questionnaire answers."""
    def get(key: str) -> str:
        val = answers.get(key, "")
        return val if val else "не указано"

    lines = [
        f"Тип скана: {scan_type}",
        f"Число души: {soul_number}",
        f"Число жизненного пути: {life_path_number}",
        "",
        "Ответы на анкету:",
        f"Дата рождения: {answers.get('birth_date', 'не указано')}",
        f"Имя: {get('name')}",
        f"Сфера: {get('business_area')}",
        f"Возраст бизнеса: {get('business_age')}",
        f"Роль: {get('role')}",
        f"Команда: {get('team_size')}",
        f"Источник клиентов: {get('client_source')}",
        f"Средний чек: {get('avg_check')}",
        f"Главная боль: {get('main_pain')}",
        f"Блокер роста: {get('growth_blocker')}",
    ]

    if scan_type == "personal":
        lines += [
            f"Суперсила: {get('superpower')}",
            f"Стиль решений: {get('decision_style')}",
            f"Цель на год: {get('year_goal')}",
            f"Текущая ситуация: {get('current_situation')}",
        ]
    else:  # business
        lines.append(f"Описание продукта: {get('product_description')}")

    lines.append(f"Соцсеть/сайт: {get('social_url')}")

    return "\n".join(lines)
```

Write tests first (TDD):

tests/test_full_scan_ai_service.py:
- Import calculate_life_path_number, FullScanAIService
- Test calculate_life_path_number(date(1990, 5, 15)) == 3
- Test calculate_life_path_number(date(2000, 1, 1)) == 2  (day=1, month=1, year=2+0+0+0=2; 1+1+2=4... verify actual)
- Test generate_full_report happy path: mock self._client via patch("app.services.full_scan_ai_service.anthropic.AsyncAnthropic"), make messages.create return mock with content=[Mock(text=json.dumps({k: "test" for k in BLOCK_KEYS}))] and usage=Mock(input_tokens=100, output_tokens=200); assert all 6 BLOCK_KEYS in result; assert result["numerology"]["soul_number"] == calculate_soul_number(date(1990,5,15)); assert result["token_usage"]["input_tokens"] == 100
- Test ValueError on bad JSON: make content[0].text = "not json", assert raises ValueError
- Test thin answers: answers with all empty strings still returns dict with all keys (because of the .get() fallback in _build_user_prompt and the key fallback in generate_full_report)
  </action>

  <verify>
    <automated>cd /Users/yulya-reva/reva-scanner && python -m pytest tests/test_full_scan_ai_service.py -v 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - File app/services/full_scan_ai_service.py exists
    - `grep -n "calculate_life_path_number" app/services/full_scan_ai_service.py` returns a def line
    - `grep -n "claude-sonnet" app/services/full_scan_ai_service.py` returns exactly one match on the model= line
    - `grep -n "max_tokens=4000" app/services/full_scan_ai_service.py` returns a match
    - `grep -n "Full-scan tokens" app/services/full_scan_ai_service.py` returns a match
    - `grep -n "json.loads" app/services/full_scan_ai_service.py` returns a match
    - `grep -n "недостаточно данных" app/services/full_scan_ai_service.py` appears in BLOCK_KEYS fallback
    - All tests in tests/test_full_scan_ai_service.py pass
    - calculate_life_path_number(date(1990, 5, 15)) returns 3
  </acceptance_criteria>

  <done>FullScanAIService.generate_full_report() exists, calls claude-sonnet-4-5 with max_tokens=4000, returns dict with all 6 block keys + numerology + token_usage, raises ValueError on bad JSON, and all unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update scan.report to JSONB and add ScanService.complete_full_scan()</name>
  <files>app/models/scan.py, app/services/scan_service.py</files>

  <read_first>
    - app/models/scan.py (current report column — must change from Text to JSONB)
    - app/services/scan_service.py (complete_mini_scan pattern to follow for complete_full_scan)
    - app/models/scan.py (ScanStatus enum — need completed and questionnaire_complete values)
  </read_first>

  <action>
In app/models/scan.py:

1. Remove `Text` from the import (keep it only if used elsewhere; check — mini_report still uses Text so keep Text import).
2. Change report column from:
   `report: Mapped[Optional[str]] = mapped_column(Text, nullable=True)`
   to:
   `report: Mapped[Optional[dict]] = mapped_column(JSONB, nullable=True)`

In app/services/scan_service.py, add after complete_questionnaire():

```python
async def complete_full_scan(
    self,
    scan_id: int,
    report: dict,
    token_usage: dict,
) -> Scan:
    """Store the full AI report as JSONB, mark scan as completed.

    Args:
        scan_id: Primary key of the Scan to finalize.
        report: Full 6-block report dict from FullScanAIService.generate_full_report().
                token_usage is embedded inside report under "token_usage" key.
        token_usage: Passed separately for explicit logging; merged into report if missing.

    Returns:
        The updated and refreshed Scan instance.
    """
    scan = await self._get_or_raise(scan_id)
    # Embed token_usage in report for completeness
    if "token_usage" not in report:
        report = {**report, "token_usage": token_usage}
    scan.report = report
    scan.status = ScanStatus.completed.value
    scan.completed_at = datetime.now(timezone.utc)
    await self.session.commit()
    await self.session.refresh(scan)
    return scan
```

Add tests to tests/test_scan_service.py (add at the bottom of the file — do NOT overwrite existing tests):
- test_complete_full_scan_sets_report_and_status: mock session, mock scan with id=1 status=questionnaire_complete; call complete_full_scan(1, report_dict, token_usage); assert scan.report == report_dict, scan.status == ScanStatus.completed.value, scan.completed_at is not None, session.commit called once.
  </action>

  <verify>
    <automated>cd /Users/yulya-reva/reva-scanner && python -m pytest tests/test_scan_service.py -v -k "complete_full_scan" 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "report.*JSONB\|JSONB.*report" app/models/scan.py` returns a match (report column is JSONB)
    - `grep -n "Mapped\[Optional\[dict\]\]" app/models/scan.py` returns a match on the report line
    - `grep -n "def complete_full_scan" app/services/scan_service.py` returns a match
    - `grep -n "scan.report = report" app/services/scan_service.py` returns a match
    - `grep -n "ScanStatus.completed.value" app/services/scan_service.py` returns a match in complete_full_scan body
    - `python -m pytest tests/test_scan_service.py -v 2>&1 | grep -E "PASSED|FAILED"` — all existing tests still PASSED plus new complete_full_scan test PASSED
  </acceptance_criteria>

  <done>scan.report is JSONB, ScanService.complete_full_scan() stores dict and sets status=completed, all scan_service tests pass.</done>
</task>

</tasks>

<verification>
Run full test suite to confirm nothing broken:

```bash
cd /Users/yulya-reva/reva-scanner && python -m pytest tests/ -v 2>&1 | tail -30
```

Expected: all pre-existing tests pass + new AI service tests pass. Zero failures.

Confirm model change:
```bash
grep -n "report" /Users/yulya-reva/reva-scanner/app/models/scan.py
```
Expected output: `report: Mapped[Optional[dict]] = mapped_column(JSONB, nullable=True)`
</verification>

<success_criteria>
1. generate_full_report() exists and returns dict with exactly: архитектура, слепые_зоны, энергетические_блоки, команда, деньги, рекомендации, numerology, token_usage.
2. calculate_life_path_number() exists and returns correct single digit.
3. scan.report column is JSONB (dict), not Text (str).
4. ScanService.complete_full_scan() stores report dict and sets status=completed.
5. All tests pass (pytest tests/ exits 0).
</success_criteria>

<output>
After completion, create .planning/phases/04-ai-engine-and-report-delivery/04-01-SUMMARY.md
</output>
