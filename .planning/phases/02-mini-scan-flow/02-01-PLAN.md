---
phase: 02-mini-scan-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/ai_service.py
  - app/services/scan_service.py
  - app/services/numerology.py
  - requirements.txt
  - tests/test_numerology.py
  - tests/test_scan_service.py
autonomous: true
requirements:
  - AI-01
  - AI-04
  - AI-05

must_haves:
  truths:
    - "Numerology soul number calculation returns correct single digit for any date"
    - "Claude Haiku generates a teaser report from 5 answers and soul number"
    - "Scan record is created and updated in DB with answers, mini_report, and status"
    - "Token usage is logged per AI request"
  artifacts:
    - path: "app/services/numerology.py"
      provides: "Soul number calculation from birth date"
      exports: ["calculate_soul_number"]
    - path: "app/services/ai_service.py"
      provides: "Claude API wrapper for mini-scan teaser generation"
      exports: ["AIService"]
    - path: "app/services/scan_service.py"
      provides: "Scan CRUD operations"
      exports: ["ScanService"]
    - path: "tests/test_numerology.py"
      provides: "Unit tests for numerology calculation"
    - path: "tests/test_scan_service.py"
      provides: "Unit tests for scan service"
  key_links:
    - from: "app/services/ai_service.py"
      to: "anthropic API"
      via: "anthropic.AsyncAnthropic client"
      pattern: "client\\.messages\\.create"
    - from: "app/services/ai_service.py"
      to: "app/services/numerology.py"
      via: "import calculate_soul_number"
      pattern: "calculate_soul_number"
    - from: "app/services/scan_service.py"
      to: "app/models/scan.py"
      via: "SQLAlchemy ORM queries"
      pattern: "Scan\\("
---

<objective>
Build the three core backend services for the mini-scan: numerology calculator, Claude AI teaser generator, and scan persistence service.

Purpose: These services are the building blocks that the FSM handler (Plan 02) will call. Building them first allows isolated testing without Telegram bot complexity.
Output: Three service modules with tests, anthropic added to requirements.txt.
</objective>

<execution_context>
@/Users/yulya-reva/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yulya-reva/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing codebase contracts the executor needs -->

From app/models/scan.py:
```python
class ScanType(str, Enum):
    mini = "mini"
    personal = "personal"
    business = "business"

class ScanStatus(str, Enum):
    pending = "pending"
    collecting = "collecting"
    in_progress = "in_progress"
    completed = "completed"
    failed = "failed"

class Scan(Base):
    __tablename__ = "scans"
    id: Mapped[int] (primary_key)
    user_id: Mapped[int] (ForeignKey "users.id")
    scan_type: Mapped[str] (String(32))
    status: Mapped[str] (String(32), default=ScanStatus.pending)
    answers: Mapped[Optional[dict]] (JSONB)
    numerology: Mapped[Optional[dict]] (JSONB)
    report: Mapped[Optional[str]] (Text)
    mini_report: Mapped[Optional[str]] (Text)
    is_paid: Mapped[bool] (default=False)
    payment_id: Mapped[Optional[int]]
    created_at: Mapped[datetime]
    completed_at: Mapped[Optional[datetime]]
```

From app/models/user.py:
```python
class User(Base):
    __tablename__ = "users"
    id: Mapped[int] (primary_key)
    telegram_id: Mapped[int] (BigInteger, unique, index)
    username: Mapped[Optional[str]]
    full_name: Mapped[Optional[str]]
    birth_date: Mapped[Optional[date]] (Date, nullable)
    created_at: Mapped[datetime]
    updated_at: Mapped[datetime]
```

From app/config.py:
```python
class Settings(BaseSettings):
    anthropic_api_key: str = ""
    # ... other fields
settings = Settings()
```

From app/database.py:
```python
class Base(DeclarativeBase): pass
engine = create_async_engine(...)
async_session_factory = async_sessionmaker(...)
async def get_db_session() -> AsyncSession: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Numerology calculator and AI service</name>
  <files>app/services/numerology.py, app/services/ai_service.py, requirements.txt, tests/test_numerology.py</files>
  <read_first>
    - app/config.py
    - app/models/scan.py
    - app/models/user.py
    - requirements.txt
  </read_first>
  <behavior>
    - calculate_soul_number(date(1990, 5, 15)) returns 3 (1+9+9+0+0+5+1+5=30, 3+0=3)
    - calculate_soul_number(date(1999, 9, 9)) returns 9 (1+9+9+9+0+9+0+9=46, 4+6=10, 1+0=1) — wait, recalculate: soul number uses day only by some systems, but per project spec "sum digits until single digit, reduce". Using full date: 1+9+9+9+9+9=46, 4+6=10, 1+0=1. Returns 1.
    - calculate_soul_number(date(2000, 1, 1)) returns 4 (2+0+0+0+0+1+0+1=4)
    - calculate_soul_number returns int between 1 and 9
    - AIService.generate_mini_report(answers, soul_number) returns a string of 3-4 sentences
    - AIService logs token usage (input_tokens, output_tokens) via logger
    - AI prompt instructs Claude to name ONE specific uncomfortable business truth
    - AI prompt includes "недостаточно данных" instruction for thin answers (AI-04)
  </behavior>
  <action>
    1. Add `anthropic>=0.49.0` to requirements.txt (keep existing deps, just append).

    2. Create `app/services/numerology.py` with:
       - `def calculate_soul_number(birth_date: date) -> int` — sum ALL digits of the date (YYYYMMDD format), reduce repeatedly until single digit (1-9). Do NOT use modulo 9 shortcut (it maps 9 to 0). Use a while loop: while number > 9, sum its digits.

    3. Create `app/services/ai_service.py` with class `AIService`:
       - `__init__(self)` — create `anthropic.AsyncAnthropic(api_key=settings.anthropic_api_key)`
       - `async def generate_mini_report(self, answers: dict, soul_number: int) -> tuple[str, dict]` — returns (report_text, usage_dict)
       - Uses model `"claude-haiku-4-20250514"` (Haiku for mini-scan, per AI-05 cost constraint)
       - max_tokens=500 (keep response short, 3-4 sentences)
       - System prompt (in Russian): instructs Claude to act as a business diagnostic AI. Given the user's answers and soul number, name ONE specific uncomfortable business truth. Not a generic tip — a specific pain point. If answers are thin, say "недостаточно данных для точного анализа" for that aspect (AI-04). Response must be 3-4 sentences, direct, in Russian, using "ты" form.
       - User prompt: format all 5 answers plus soul number into a structured message
       - After API call: log token usage via `logging.getLogger(__name__).info(f"Mini-scan tokens: input={usage.input_tokens}, output={usage.output_tokens}")`
       - Return (response.content[0].text, {"input_tokens": usage.input_tokens, "output_tokens": usage.output_tokens})

    4. Create `tests/test_numerology.py` with pytest tests for soul number calculation covering edge cases (date with lots of 9s, date with zeros, typical dates).

    The system prompt for Claude (exact text to use):
    ```
    Ты — AI-сканер бизнеса «Глаз Бога». Твоя задача — на основе ответов пользователя назвать ОДНУ конкретную, неудобную правду о его бизнесе. Не дай общий совет — укажи на конкретную болевую точку, которую он предпочитает не замечать.

    Правила:
    - Ответ: 3-4 предложения, прямой тон, на «ты»
    - Если данных недостаточно для точного вывода — прямо скажи «недостаточно данных для точного анализа этого аспекта»
    - Используй число души пользователя для персонализации (но не упоминай нумерологию явно)
    - Будь конкретным и резким — пользователь пришёл за правдой, не за комплиментами
    ```

    The user prompt template:
    ```
    Число души: {soul_number}
    Сфера бизнеса: {business_area}
    Возраст бизнеса: {business_age}
    Главная боль: {main_pain}
    Описание ситуации: {situation or "не указано"}
    ```
  </action>
  <verify>
    <automated>cd /Users/yulya-reva/reva-scanner && python -m pytest tests/test_numerology.py -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "anthropic" requirements.txt returns 0
    - grep -q "calculate_soul_number" app/services/numerology.py returns 0
    - grep -q "class AIService" app/services/ai_service.py returns 0
    - grep -q "claude-haiku" app/services/ai_service.py returns 0
    - grep -q "недостаточно данных" app/services/ai_service.py returns 0
    - grep -q "input_tokens" app/services/ai_service.py returns 0
    - python -m pytest tests/test_numerology.py passes all tests
    - calculate_soul_number returns int, verified by test assertions
  </acceptance_criteria>
  <done>Numerology calculator passes all unit tests. AI service is wired to Claude Haiku with proper prompt, token logging, and cost-aware max_tokens. anthropic added to requirements.txt.</done>
</task>

<task type="auto">
  <name>Task 2: Scan service for DB persistence</name>
  <files>app/services/scan_service.py, app/services/__init__.py, tests/test_scan_service.py</files>
  <read_first>
    - app/models/scan.py
    - app/models/user.py
    - app/services/user_service.py
    - app/database.py
  </read_first>
  <action>
    Create `app/services/scan_service.py` with class `ScanService` following the same pattern as UserService (takes AsyncSession in __init__):

    Methods:
    1. `async def create_mini_scan(self, user_id: int) -> Scan` — creates Scan with scan_type=ScanType.mini.value, status=ScanStatus.collecting.value. Returns the new Scan.

    2. `async def update_answers(self, scan_id: int, answers: dict) -> Scan` — updates scan.answers JSONB field, sets status=ScanStatus.in_progress.value. Commits and returns updated scan.

    3. `async def complete_mini_scan(self, scan_id: int, mini_report: str, numerology: dict, token_usage: dict) -> Scan` — sets scan.mini_report, scan.numerology (store {"soul_number": N, "token_usage": token_usage}), scan.status=ScanStatus.completed.value, scan.completed_at=datetime.now(timezone.utc). Commits and returns scan.

    4. `async def get_scan(self, scan_id: int) -> Optional[Scan]` — simple get by id.

    Update `app/services/__init__.py` to export ScanService, UserService, AIService, calculate_soul_number:
    ```python
    from app.services.user_service import UserService
    from app.services.scan_service import ScanService
    from app.services.ai_service import AIService
    from app.services.numerology import calculate_soul_number
    ```

    Create `tests/test_scan_service.py` with tests using aiosqlite in-memory DB (same pattern as existing tests if any, otherwise create async fixtures with in-memory SQLite):
    - Test create_mini_scan sets correct type and status
    - Test update_answers stores JSONB and changes status
    - Test complete_mini_scan sets all fields correctly
  </action>
  <verify>
    <automated>cd /Users/yulya-reva/reva-scanner && python -m pytest tests/test_scan_service.py -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class ScanService" app/services/scan_service.py returns 0
    - grep -q "create_mini_scan" app/services/scan_service.py returns 0
    - grep -q "complete_mini_scan" app/services/scan_service.py returns 0
    - grep -q "ScanService" app/services/__init__.py returns 0
    - grep -q "AIService" app/services/__init__.py returns 0
    - python -m pytest tests/test_scan_service.py passes all tests
  </acceptance_criteria>
  <done>ScanService creates, updates, and completes mini-scans in the database. All CRUD methods tested with in-memory SQLite. Services __init__.py exports all new services.</done>
</task>

</tasks>

<verification>
- All tests pass: `python -m pytest tests/ -v`
- No import errors: `python -c "from app.services import ScanService, AIService, calculate_soul_number"`
- anthropic is in requirements.txt
- AI service uses claude-haiku model (cost constraint AI-05)
- AI prompt includes "недостаточно данных" fallback (AI-04)
</verification>

<success_criteria>
- Numerology calculator correctly reduces any date to a single digit (1-9)
- AI service calls Claude Haiku with proper Russian prompt and logs token usage
- Scan service performs full lifecycle: create -> update answers -> complete with report
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-mini-scan-flow/02-01-SUMMARY.md`
</output>
