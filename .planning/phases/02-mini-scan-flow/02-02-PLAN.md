---
phase: 02-mini-scan-flow
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/bot/handlers/start.py
  - app/bot/handlers/mini_scan.py
  - app/bot/states.py
  - app/bot/router.py
  - app/services/user_service.py
  - tests/test_mini_scan_flow.py
autonomous: true
requirements:
  - BOT-02
  - BOT-04
  - BOT-05
  - BOT-06
  - BOT-07

must_haves:
  truths:
    - "User taps /start and sees two inline buttons for scan types"
    - "User taps a scan button and enters the 5-question mini-scan FSM"
    - "User enters birth date as text and it is stored in User.birth_date"
    - "User selects business area, business age, and main pain via inline keyboard buttons"
    - "User can type optional situation text or skip with a button"
    - "User sees a 'сканирую...' message immediately after submitting"
    - "User receives a 3-4 sentence teaser report naming one business pain"
    - "User sees an upsell button after the teaser report"
    - "Scan record in DB has status=completed, answers as JSONB, mini_report as text"
  artifacts:
    - path: "app/bot/states.py"
      provides: "FSM state group for mini-scan"
      exports: ["MiniScanStates"]
    - path: "app/bot/handlers/mini_scan.py"
      provides: "FSM handlers for 5-question mini-scan flow"
      min_lines: 100
    - path: "app/bot/handlers/start.py"
      provides: "Updated /start with inline keyboard buttons"
    - path: "tests/test_mini_scan_flow.py"
      provides: "Integration tests for mini-scan FSM"
  key_links:
    - from: "app/bot/handlers/mini_scan.py"
      to: "app/services/ai_service.py"
      via: "AIService.generate_mini_report call"
      pattern: "generate_mini_report"
    - from: "app/bot/handlers/mini_scan.py"
      to: "app/services/scan_service.py"
      via: "ScanService CRUD calls"
      pattern: "ScanService"
    - from: "app/bot/handlers/mini_scan.py"
      to: "app/services/user_service.py"
      via: "UserService.update_birth_date"
      pattern: "update_birth_date"
    - from: "app/bot/handlers/start.py"
      to: "app/bot/handlers/mini_scan.py"
      via: "Inline keyboard callback triggers FSM"
      pattern: "scan_type:"
---

<objective>
Build the complete mini-scan Telegram conversation flow: update /start with inline keyboard, implement 5-question FSM, show "сканирую..." feedback, call AI service for teaser, display report with upsell button.

Purpose: This is the user-facing mini-scan experience — the free funnel that hooks users and drives paid conversion.
Output: Working mini-scan flow from /start through teaser report with upsell CTA.
</objective>

<execution_context>
@/Users/yulya-reva/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yulya-reva/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mini-scan-flow/02-01-SUMMARY.md

<interfaces>
<!-- Services created in Plan 01 that this plan wires into handlers -->

From app/services/numerology.py:
```python
def calculate_soul_number(birth_date: date) -> int:
    """Sum all digits of YYYYMMDD, reduce to single digit 1-9."""
```

From app/services/ai_service.py:
```python
class AIService:
    def __init__(self): ...
    async def generate_mini_report(self, answers: dict, soul_number: int) -> tuple[str, dict]:
        """Returns (report_text, {"input_tokens": N, "output_tokens": N})"""
```

From app/services/scan_service.py:
```python
class ScanService:
    def __init__(self, session: AsyncSession): ...
    async def create_mini_scan(self, user_id: int) -> Scan: ...
    async def update_answers(self, scan_id: int, answers: dict) -> Scan: ...
    async def complete_mini_scan(self, scan_id: int, mini_report: str, numerology: dict, token_usage: dict) -> Scan: ...
```

From app/services/user_service.py:
```python
class UserService:
    def __init__(self, session: AsyncSession): ...
    async def get_or_create(self, telegram_id, username, full_name) -> Tuple[User, bool]: ...
    async def get_by_telegram_id(self, telegram_id) -> Optional[User]: ...
```

From app/models/scan.py:
```python
class ScanType(str, Enum): mini = "mini", personal = "personal", business = "business"
class ScanStatus(str, Enum): pending, collecting, in_progress, completed, failed
```

From app/bot/router.py:
```python
main_router = Router(name="main")
main_router.include_router(start.router)
```

From app/main.py:
```python
dp = Dispatcher()
dp.include_router(main_router)
# session is passed via dp.feed_update(bot, update, session=session)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FSM states, /start update, and birth date handler</name>
  <files>app/bot/states.py, app/bot/handlers/start.py, app/bot/handlers/mini_scan.py, app/bot/handlers/__init__.py, app/services/user_service.py</files>
  <read_first>
    - app/bot/handlers/start.py
    - app/bot/router.py
    - app/main.py
    - app/services/user_service.py
    - app/models/user.py
    - app/models/scan.py
  </read_first>
  <action>
    1. Create `app/bot/states.py` with FSM state group:
       ```python
       from aiogram.fsm.state import State, StatesGroup

       class MiniScanStates(StatesGroup):
           birth_date = State()        # Q1: waiting for birth date text input
           business_area = State()     # Q2: inline keyboard
           business_age = State()      # Q3: inline keyboard
           main_pain = State()         # Q4: inline keyboard
           situation = State()         # Q5: optional text or skip
           generating = State()        # Processing state
       ```

    2. Update `app/bot/handlers/start.py`:
       - Import InlineKeyboardButton, InlineKeyboardMarkup from aiogram.types
       - Replace the plain text response with an inline keyboard containing 2 buttons:
         - `InlineKeyboardButton(text="Личное сканирование 3500⭐", callback_data="scan_type:personal")`
         - `InlineKeyboardButton(text="Бизнес-сканирование 7000⭐", callback_data="scan_type:business")`
       - Add a third button on a separate row: `InlineKeyboardButton(text="🔍 Бесплатный мини-скан", callback_data="scan_type:mini")`
       - The welcome text remains the same ("Глаз Бога" greeting), but add a line: "Начни с бесплатного мини-скана — узнай одну неудобную правду о своём бизнесе."
       - Import FSMContext, add it as parameter to cmd_start, call `await state.clear()` at the beginning to reset any stale FSM state

    3. Add `update_birth_date` method to `app/services/user_service.py`:
       ```python
       async def update_birth_date(self, telegram_id: int, birth_date: date) -> Optional[User]:
           result = await self.session.execute(
               select(User).where(User.telegram_id == telegram_id)
           )
           user = result.scalar_one_or_none()
           if user:
               user.birth_date = birth_date
               await self.session.commit()
               await self.session.refresh(user)
           return user
       ```

    4. Create `app/bot/handlers/mini_scan.py` with the FIRST HALF of the flow (questions 1-2):
       - Create a Router named "mini_scan"
       - Import MiniScanStates, ScanService, UserService, all needed aiogram types
       - Callback handler for `callback_data="scan_type:mini"`:
         - Get or create user via UserService
         - Create mini scan via ScanService.create_mini_scan(user.id)
         - Store scan_id in FSM data: `await state.update_data(scan_id=scan.id, user_id=user.id, answers={})`
         - Set state to MiniScanStates.birth_date
         - Answer callback with message: "📅 Вопрос 1 из 5\n\nВведите вашу дату рождения в формате ДД.ММ.ГГГГ\n\nНапример: 15.05.1990"
         - Call `await callback.answer()` to dismiss the loading indicator
       - For callback_data="scan_type:personal" and "scan_type:business":
         - Answer with: "Полное сканирование будет доступно после мини-скана. Начните с бесплатного мини-скана!"
         - (Payment flow is Phase 5)
       - Message handler for birth_date state:
         - Parse date from text using format DD.MM.YYYY (use `datetime.strptime(text.strip(), "%d.%m.%Y").date()`)
         - If invalid format: reply "Неверный формат. Введите дату в формате ДД.ММ.ГГГГ (например: 15.05.1990)" and stay in same state
         - If valid: save birth_date via `UserService.update_birth_date(telegram_id, parsed_date)`
         - Store birth_date in FSM data
         - Show question 2 with inline keyboard:
           "📊 Вопрос 2 из 5\n\nВаша основная сфера бизнеса?"
           Buttons (each on its own row): "Услуги" (cb: area:services), "Продукты" (cb: area:products), "IT" (cb: area:it), "Торговля" (cb: area:trade), "Другое" (cb: area:other)
         - Set state to MiniScanStates.business_area

    5. Update `app/bot/router.py` to include the mini_scan router:
       ```python
       from app.bot.handlers import start, mini_scan
       main_router.include_router(mini_scan.router)
       ```

    6. Update `app/bot/handlers/__init__.py` if needed (may be empty, just ensure mini_scan is importable).
  </action>
  <verify>
    <automated>cd /Users/yulya-reva/reva-scanner && python -c "from app.bot.states import MiniScanStates; from app.bot.handlers.mini_scan import router; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class MiniScanStates" app/bot/states.py returns 0
    - grep -q "birth_date" app/bot/states.py returns 0
    - grep -q "scan_type:mini" app/bot/handlers/start.py returns 0
    - grep -q "InlineKeyboardButton" app/bot/handlers/start.py returns 0
    - grep -q "Бесплатный мини-скан" app/bot/handlers/start.py returns 0
    - grep -q "update_birth_date" app/services/user_service.py returns 0
    - grep -q "scan_type:mini" app/bot/handlers/mini_scan.py returns 0
    - grep -q "area:services" app/bot/handlers/mini_scan.py returns 0
    - grep -q "mini_scan" app/bot/router.py returns 0
    - python -c "from app.bot.states import MiniScanStates" exits 0
  </acceptance_criteria>
  <done>/start shows inline keyboard with 3 buttons (personal, business, mini-scan). Tapping mini-scan enters FSM, asks birth date (text input), validates format, stores to User.birth_date, then shows business area question with 5 inline buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Questions 3-5, AI generation with scanning feedback, and upsell</name>
  <files>app/bot/handlers/mini_scan.py, tests/test_mini_scan_flow.py</files>
  <read_first>
    - app/bot/handlers/mini_scan.py
    - app/bot/states.py
    - app/services/ai_service.py
    - app/services/scan_service.py
    - app/services/numerology.py
    - app/models/scan.py
  </read_first>
  <action>
    Continue building `app/bot/handlers/mini_scan.py` — add handlers for questions 2-5 completion, AI generation, and upsell:

    1. Callback handler for business_area state (callback_data starts with "area:"):
       - Extract area value from callback_data (split on ":")
       - Map callback values to Russian labels: {"services": "Услуги", "products": "Продукты", "it": "IT", "trade": "Торговля", "other": "Другое"}
       - Store in FSM data: answers["business_area"] = russian_label
       - Show question 3: "⏳ Вопрос 3 из 5\n\nСколько лет вашему бизнесу?"
       - Buttons: "< 1 года" (cb: age:lt1), "1–3 года" (cb: age:1to3), "3–7 лет" (cb: age:3to7), "7+ лет" (cb: age:7plus)
       - Set state to MiniScanStates.business_age
       - Call `await callback.answer()`

    2. Callback handler for business_age state (callback_data starts with "age:"):
       - Map: {"lt1": "< 1 года", "1to3": "1–3 года", "3to7": "3–7 лет", "7plus": "7+ лет"}
       - Store answers["business_age"]
       - Show question 4: "🔥 Вопрос 4 из 5\n\nВаша главная боль прямо сейчас?"
       - Buttons: "Нет клиентов" (cb: pain:no_clients), "Нет команды" (cb: pain:no_team), "Нет системы" (cb: pain:no_system), "Нет денег" (cb: pain:no_money), "Всё сразу" (cb: pain:everything)
       - Set state to MiniScanStates.main_pain
       - Call `await callback.answer()`

    3. Callback handler for main_pain state (callback_data starts with "pain:"):
       - Map: {"no_clients": "Нет клиентов", "no_team": "Нет команды", "no_system": "Нет системы", "no_money": "Нет денег", "everything": "Всё сразу"}
       - Store answers["main_pain"]
       - Show question 5: "💬 Вопрос 5 из 5\n\nКратко опишите вашу ситуацию (до 500 символов)\n\nИли нажмите «Пропустить»"
       - Buttons: just one — "Пропустить" (cb: situation:skip)
       - Set state to MiniScanStates.situation
       - Call `await callback.answer()`

    4. Two handlers for situation state:
       a. Callback handler for "situation:skip":
          - Store answers["situation"] = "" (empty)
          - Call the generation function (see step 5)
       b. Message handler (text input):
          - Truncate to 500 chars: text = message.text[:500]
          - Store answers["situation"] = text
          - Call the generation function (see step 5)

    5. Create an async helper function `_generate_and_send_report(message_or_callback, state, session)`:
       - Get FSM data (scan_id, user_id, answers, birth_date)
       - IMMEDIATELY send "сканирую..." message: `scanning_msg = await bot.send_message(chat_id, "🔮 Сканирую...")`
         (Import bot from app.main — or better, pass it via callback/message.bot)
       - Update scan answers in DB via ScanService.update_answers(scan_id, answers)
       - Calculate soul_number from birth_date via calculate_soul_number
       - Call AIService().generate_mini_report(answers, soul_number) -> (report_text, token_usage)
       - Complete scan via ScanService.complete_mini_scan(scan_id, report_text, {"soul_number": soul_number}, token_usage)
       - Delete the "сканирую..." message: `await scanning_msg.delete()`
       - Send the teaser report: `await bot.send_message(chat_id, f"👁 <b>Результат мини-скана</b>\n\n{report_text}", parse_mode="HTML")`
       - Send upsell message with inline keyboard:
         ```
         "Это лишь верхушка айсберга. Полный скан раскроет 6 блоков: архитектуру бизнеса, слепые зоны, энергетические блоки, команду, деньги и персональные рекомендации."
         ```
         With two buttons:
         - "Получить полный скан 3500⭐" (cb: "buy:personal")
         - "Получить полный скан 7000⭐" (cb: "buy:business")
       - Clear FSM state: `await state.clear()`
       - Wrap the AI call in try/except: on any exception, delete scanning_msg, send "Произошла ошибка при генерации. Попробуйте ещё раз через /start", log the error, clear state

    6. Create `tests/test_mini_scan_flow.py`:
       - Test that MiniScanStates has all 6 states
       - Test callback data patterns are consistent (area:, age:, pain:, situation:)
       - Test date parsing logic (extract into a helper function in mini_scan.py for testability): valid "15.05.1990" returns date(1990,5,15), invalid "abc" raises ValueError
  </action>
  <verify>
    <automated>cd /Users/yulya-reva/reva-scanner && python -m pytest tests/test_mini_scan_flow.py -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Сканирую" app/bot/handlers/mini_scan.py returns 0
    - grep -q "generate_mini_report" app/bot/handlers/mini_scan.py returns 0
    - grep -q "calculate_soul_number" app/bot/handlers/mini_scan.py returns 0
    - grep -q "complete_mini_scan" app/bot/handlers/mini_scan.py returns 0
    - grep -q "buy:personal" app/bot/handlers/mini_scan.py returns 0
    - grep -q "buy:business" app/bot/handlers/mini_scan.py returns 0
    - grep -q "Получить полный скан" app/bot/handlers/mini_scan.py returns 0
    - grep -q "Пропустить" app/bot/handlers/mini_scan.py returns 0
    - grep -q "pain:no_clients" app/bot/handlers/mini_scan.py returns 0
    - grep -q "age:lt1" app/bot/handlers/mini_scan.py returns 0
    - grep -c "await callback.answer()" app/bot/handlers/mini_scan.py returns at least 4
    - python -m pytest tests/test_mini_scan_flow.py passes all tests
  </acceptance_criteria>
  <done>Complete mini-scan FSM flow works: 5 questions (birth date text + 3 inline keyboards + optional text), immediate "сканирую..." feedback, Claude Haiku teaser generation, report display, upsell buttons. Scan persisted in DB with answers, mini_report, numerology, and token usage. Error handling prevents frozen bot.</done>
</task>

</tasks>

<verification>
- All tests pass: `python -m pytest tests/ -v`
- Import chain works: `python -c "from app.bot.handlers.mini_scan import router; from app.bot.states import MiniScanStates; print('OK')"`
- /start handler shows 3 inline buttons (personal 3500, business 7000, mini-scan free)
- Mini-scan FSM has 6 states covering all 5 questions + generation
- "Сканирую..." message is sent BEFORE AI call (no frozen bot)
- Upsell buttons appear after teaser report
- Birth date is stored in User.birth_date via UserService.update_birth_date
- Scan record stores: answers (JSONB), mini_report (text), numerology (JSONB with soul_number), status=completed
- Token usage logged per request
</verification>

<success_criteria>
- User can navigate the entire mini-scan via inline keyboard buttons (no free-text except birth date and optional situation)
- Birth date is parsed, validated, and stored in User.birth_date
- "Сканирую..." feedback appears immediately after last answer
- Teaser report is 3-4 sentences naming one specific business pain
- Upsell buttons show correct prices (3500/7000 stars)
- All requirement IDs addressed: BOT-02, BOT-04, BOT-05, BOT-06, BOT-07
</success_criteria>

<output>
After completion, create `.planning/phases/02-mini-scan-flow/02-02-SUMMARY.md`
</output>
